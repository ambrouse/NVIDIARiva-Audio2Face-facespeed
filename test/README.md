# Release Evidence

This folder stores human-reviewable release evidence.

Current package:

- `release-readiness-2026-05-23/`

Keep only the latest useful evidence package unless there is an explicit reason to compare releases. Automated test source files belong in `../tests/`.
