# Port Forward Logs

Thư mục này dành cho log tạm của port-forward hoặc bridge thủ công nếu có.

Không lưu báo cáo task ở đây. Nếu bridge thuộc benchmark, ghi summary ở `logs/benchmarks/` và evidence ở `tests/benchmarks/`.
