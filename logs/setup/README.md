# Setup Runtime Logs

Thư mục này chứa log tạm từ setup/runtime cũ nếu có.

Luồng mới ưu tiên tmux và thông báo trực tiếp trên terminal. Các file `.log` trong thư mục này có thể được dọn bằng `scripts/manage-logs.sh --clean` nếu không cần tra cứu lỗi cũ.
